{
  "name": "pik",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./src/public/styles/main.css -o ./dist/output.css",
    "build": "rm -fr ./dist && bun build ./src/app.ts --outdir ./dist --sourcemap && bun build ./src/service-worker.ts --outdir ./dist --sourcemap && bun build ./src/content-script.ts --outdir ./dist --sourcemap && cp -r src/public/* dist/ && rm -rf dist/styles && cp manifest.json dist/ && npm run build:css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.13",
    "@types/bun": "latest",
    "@types/chrome": "^0.1.4",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.13"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
